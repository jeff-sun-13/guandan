{
  "name": "guandan",
  "private": true,
  "version": "0.0.0",
  "description": "Mobile-friendly Guandan with strong bots. See CLAUDE.md and docs/.",
  "scripts": {
    "dev": "pnpm --filter @guandan/web dev",
    "build": "pnpm --filter @guandan/web build",
    "eval": "pnpm --filter @guandan/tools eval",
    "ladder": "pnpm --filter @guandan/tools ladder",
    "bench": "pnpm --filter @guandan/tools bench",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
